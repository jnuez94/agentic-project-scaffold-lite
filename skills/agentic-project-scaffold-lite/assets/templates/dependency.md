# Dependency

```yaml
dependency:
  task_id:
  depends_on_task_id:
  dependency_type: blocks
  status: active
  rationale:
```

Dependency type values:

```text
blocks
informs
review_required
evidence_required
```

Status values:

```text
active
resolved
```
